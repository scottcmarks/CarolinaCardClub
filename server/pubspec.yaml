name: carolina_card_club_server
description: A server for the Carolina Card Club application.
version: 1.0.0
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  http: ^1.2.1
  path: ^1.9.0
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  shelf_web_socket: ^1.0.4
  sqflite_common_ffi: ^2.3.3
  web_socket_channel: ^2.4.0
  shared:
    path: ../shared

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
