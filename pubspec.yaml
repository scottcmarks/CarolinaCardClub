

name: carolina_card_club
description: Carolina Card Club session management
publish_to: 'none' # You can change this if you intend to publish to pub.dev

environment:
  sdk: '>=3.3.0 <4.0.0' # Add this section
  # Or use a range compatible with your specific Flutter version,
  # for example: '>=3.3.0 <4.0.0' if you are on Flutter's latest stable release.

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  drift: ^2.14.0 # Use the latest version
  sqlite3_flutter_libs: ^0.5.18 # Use the latest version
  path_provider: ^2.1.1
  path: ^1.8.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  drift_dev: ^2.14.1 # Use the latest version
  build_runner: ^2.4.7

flutter:
  uses-material-design: true
  assets:
    - assets/CarolinaCardClub.db
